interface:
  display_name: "Deterministic Pi + Codex Harnesses"
  short_description: "Build, research, verify, and run deterministic Pi workflows"
  default_prompt: "Use $deterministic-workflow to build and verify a bounded Pi workflow for this task, including typed evidence, mechanical gates, parallel stages, and trustworthy reporting."
