# Query and Path Log

## Local paths inspected

- `schemas/workflow-blueprint.schema.json`
- `scripts/workflow_factory_common.py`
- `scripts/compile_workflow.py`
- `scripts/certify_workflow.py`
- `scripts/run_pi_harness.py`
- `extensions/harness-guard.ts`
- `references/pi-capability-map.md`
- `references/optimization-playbook.md`
- `tests/test_workflow_factory.py`

## Planned query families

- Pi JSON, RPC, SDK, extensions, sessions, custom tools, compaction, settings,
  security, packages, and model registry.
- Deterministic workflow transition conditions, ambiguity, defaults, cycles,
  retries, and event history in primary specifications/docs.
- Agent evaluator-optimizer and eval observability guidance for conditional
  routing and verified terminal outcomes.
- Contrary searches: model-chosen routing failures, workflow nondeterminism,
  expression-language injection, retry loops, and state-machine dead ends.

## Failed or unavailable routes

- The ordinary web-search provider returned no visible results for four
  source-scoped discovery queries. Research routed to direct official URLs,
  pinned GitHub source, `gh api`, and local clones/docs instead.
- No Exa Agent call was made because the primary-source universe was already
  bounded and direct official sources resolved the coverage matrix without a
  paid or asynchronous candidate-generation step.

## Sources opened and verified

- Pi `0.80.5`/`0.80.6` JSON, RPC, SDK, extensions, sessions, settings, models,
  security, packages, print-mode source, RPC-mode source, and structured-output
  example at pinned commits/releases.
- Amazon States Language specification, Choice state, terminal semantics, and
  retry semantics.
- Open Workflow DSL `1.0.0` Switch and Retry sections.
- JsonLogic core and custom-operation warnings.
- Temporal workflow replay/Event History documentation.
- Anthropic agent workflow and eval guidance.

## Convergence follow-ups

1. Compared ordered first-match standards with explicit priority/tie-failure.
   No primary source required this stricter policy; it remains a deliberate
   fail-closed inference supported by DMN hit-policy precedent.
2. Checked Pi source against rendered JSON docs. Found material differences:
   JSON-mode exit semantics and transport-specific extension-error visibility.
3. Re-audited the local compiler. Confirmed that unknown transition metadata
   previously disappeared because handwritten validation did not reject it.
